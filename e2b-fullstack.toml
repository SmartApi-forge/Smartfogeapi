# E2B Full-Stack Template Configuration
# Use this template for full-stack projects with preview capabilities

team_id = "b660529a-e319-48aa-be6d-3cbd73c07878"
dockerfile = "e2b-fullstack.Dockerfile"
template_name = "smart-forge-fullstack"
start_cmd = "/bin/true"

# To build this template:
# 1. Run: e2b template build -c e2b-fullstack.toml -d e2b-fullstack.Dockerfile
# 2. Copy the template_id from the output
# 3. Update E2B_FULLSTACK_TEMPLATE_ID in .env with the new template_id

# Usage in code:
# const sandbox = await Sandbox.create('smart-forge-fullstack')
